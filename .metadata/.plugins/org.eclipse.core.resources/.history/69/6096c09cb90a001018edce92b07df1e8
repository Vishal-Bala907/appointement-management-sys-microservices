spring:
  application:
    name: gatewat
  cloud:
    gateway:
      mvc:
        routes:
        - id: gateway
				
server:
  port: 3000

	eureka.client.service-url.defaultZone=http://localhost:8080/eureka/